# `tests/test_01_disp_base.py` 改善タスク

`Plan.md` に基づき、以下のタスクを順番に実行する。

## 1. `test_close` メソッドの分割

- [x] `test_close` メソッドを `test_close_calls_pi_stop_when_connected` にリネームし、`pi.connected` が `True` の場合のテストのみ残すように修正する。
- [x] `test_close_does_not_call_pi_stop_when_not_connected` メソッドを新しく作成し、`pi.connected` が `False` の場合のテストを実装する。

## 2. `test_display` メソッドのテストカバレッジ向上

- [x] `test_display` メソッドを `test_display_resizes_image` にリネームする。
- [x] `test_display_with_correct_size_image` メソッドを新しく作成し、画像がリサイズされないケースのテストを実装する。

## 3. 最終確認

- [x] `uv run pytest tests/test_01_disp_base.py` を実行し、すべてのテストがパスすることを確認する。
- [x] `mise run lint` を実行し、リンターエラーが出ないことを確認する。